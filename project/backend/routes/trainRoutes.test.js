
describe('Train Routes', () => {
    it('should respond with train arrival data for GET /arrival', async () => {
    // Make a mock request
    // Assert response data matches mock response data
    });
});